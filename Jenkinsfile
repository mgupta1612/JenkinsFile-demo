pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
      stage('git_version') {
            steps {
                echo 'git --version'
            }
        }
    }
}
